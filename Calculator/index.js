const add = (a, b) => a + b;
const decrese = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;

const CalculatorService = {
  add,
  decrese,
  multiply,
  divide,
};

export default CalculatorService;
